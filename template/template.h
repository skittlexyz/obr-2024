#ifndef template
#define template
#include "Arduino.h"
class template {
public:
  /* declare the constructor and the public functions and variables */
  template(int pin);
  void blink(int blinkRate);
private:
  /* declare the private functions and variables*/
  int _pin;
};
#endif